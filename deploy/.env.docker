# Docker Compose 环境变量
# 复制为 .env 并根据需要修改

# Mutong 服务端口
MUTONG_PORT=8888

# PostgreSQL
POSTGRES_USER=mutong
POSTGRES_PASSWORD=change_me
POSTGRES_DB=mutong

# Nebula Graph
NEBULA_USER=root
NEBULA_PASSWORD=change_me

# Kafka (Redpanda 兼容)
KAFKA_BROKER=kafka:9092
